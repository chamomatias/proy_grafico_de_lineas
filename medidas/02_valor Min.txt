01_valor Min = 
VAR _MinValor =
    /*
        Calcula el valor mínimo de [01_valor] dentro del contexto visible.
        ALLSELECTED preserva los filtros externos (segmentadores, filtros de página)
        y FILTER excluye fechas donde la medida base está en BLANK().
    */
    MINX (
        FILTER (
            ALLSELECTED ( '01_dim_calendario' ),
            NOT ISBLANK ( [01_valor] )
        ),
        [01_valor]
    )
RETURN
// Devuelve [01_valor] solo cuando coincide con el mínimo detectado;
// en las demás filas muestra BLANK() para evitar duplicados.
IF (
    [01_valor] = _MinValor,
    [01_valor],
    BLANK ()
)
